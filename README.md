# GreenTubeTest

for testing:
* npm i -g angular-cli
* npm i 
* ng serve --prod
* http://localhost:4200/

* ng build --prod (building production version)


Adding Unit test coverage:
* ng test --code-coverage 